"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;var _vite = await jitiImport("vite");
var _pluginReactSwc = _interopRequireDefault(await jitiImport("@vitejs/plugin-react-swc"));
var _path = _interopRequireDefault(await jitiImport("path"));
var _vitePluginPwa = await jitiImport("vite-plugin-pwa");function _interopRequireDefault(e) {return e && e.__esModule ? e : { default: e };}

const devServiceWorkerCleaner = () => ({
  name: "dev-service-worker-cleaner",
  apply: "serve",
  configureServer(server) {
    server.middlewares.use((req, res, next) => {
      if (req.url !== "/sw.js" && req.url !== "/dev-sw.js?dev-sw") return next();

      res.setHeader("Content-Type", "application/javascript");
      res.setHeader("Cache-Control", "no-store, no-cache, must-revalidate, proxy-revalidate");
      res.end(`
self.addEventListener('install', event => self.skipWaiting());
self.addEventListener('activate', event => {
  event.waitUntil((async () => {
    try {
      const keys = await caches.keys();
      await Promise.all(keys.map(key => caches.delete(key)));
    } catch (error) {}
    try {
      await self.registration.unregister();
    } catch (error) {}
    const clientsList = await self.clients.matchAll({ type: 'window', includeUncontrolled: true });
    clientsList.forEach(client => client.navigate(client.url));
  })());
});
`);
    });
  }
});

// https://vitejs.dev/config/
var _default = exports.default = (0, _vite.defineConfig)(({ mode }) => ({
  build: {
    target: "es2017",
    cssTarget: "chrome61",
    rollupOptions: {
      output: {
        manualChunks(id) {
          if (!id.includes("node_modules")) return;
          if (id.includes("@supabase")) return "supabase";
          if (id.includes("@tanstack")) return "react-query";
          if (id.includes("@radix-ui")) return "radix-ui";
          if (id.includes("lucide-react")) return "icons";
          if (id.includes("react-dom") || id.includes("/react/")) return "react";
        }
      }
    }
  },
  esbuild: {
    target: "es2017"
  },
  server: {
    host: "::",
    port: 8080,
    hmr: {
      overlay: false
    }
  },
  plugins: [
  devServiceWorkerCleaner(),
  (0, _pluginReactSwc.default)(),
  (0, _vitePluginPwa.VitePWA)({
    registerType: "autoUpdate",
    injectRegister: "auto",
    strategies: "generateSW",
    devOptions: {
      enabled: false
    },
    workbox: {
      cleanupOutdatedCaches: true,
      clientsClaim: true,
      skipWaiting: true,
      globPatterns: ["**/*.{js,css,html,ico,png,svg,woff,woff2}"],
      navigateFallback: "/index.html",
      navigateFallbackDenylist: [/^\/~oauth/],
      runtimeCaching: [
      {
        urlPattern: /^https:\/\/fonts\.googleapis\.com\/.*/i,
        handler: "CacheFirst",
        options: {
          cacheName: "google-fonts-cache",
          expiration: { maxEntries: 10, maxAgeSeconds: 60 * 60 * 24 * 365 },
          cacheableResponse: { statuses: [0, 200] }
        }
      },
      {
        urlPattern: /^https:\/\/fonts\.gstatic\.com\/.*/i,
        handler: "CacheFirst",
        options: {
          cacheName: "google-fonts-webfonts",
          expiration: { maxEntries: 20, maxAgeSeconds: 60 * 60 * 24 * 365 },
          cacheableResponse: { statuses: [0, 200] }
        }
      }]

    },
    manifest: false // Using public/manifest.json
  })].
  filter(Boolean),
  resolve: {
    alias: [
    { find: /^@\/lib\/data$/, replacement: _path.default.resolve(__dirname, "./src/lib/dataPublic.ts") },
    { find: "@", replacement: _path.default.resolve(__dirname, "./src") }],

    dedupe: ["react", "react-dom", "react/jsx-runtime", "react/jsx-dev-runtime", "@tanstack/react-query", "@tanstack/query-core"]
  }
})); /* v9-3e9f6fbc6a63da79 */
